# the 'split()' method returns a list where the text between the specified seperator becomes the list items.

# the 'split()' method splits the string into sbustrings if it finds instances of the seperator.

# the split method gives the output with in a square brackets[].



a = "Hello, world!"
print(a.split("."))